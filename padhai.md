# these commands are used to setup the author name and the mail to show each chnages and line who wrote 

git config --global user.name "name"
git config --global user.email "tanaymitra9@gmail.com"

# whenever git will face any issue our code editor which is vs code will open up 
# here code stands for vs code 
git config --global core.editor "code --wait"
#--wait means waiting 

## its used  to set up teh default line ending for mac and windows to sync in one sma epage 
git config --global core.autocrlf

changes
